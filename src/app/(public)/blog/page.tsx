export const revalidate = 3600;

export default function BlogPage() {
  return (
    <main className="container-shell py-24">
      <h1 className="font-[var(--font-geist)] text-4xl font-extrabold">Blog Agencia JN</h1>
      <p className="mt-3 text-[var(--color-ink-muted)]">Publicacao automatizada por IA sera implementada na Fase 3.</p>
    </main>
  );
}
